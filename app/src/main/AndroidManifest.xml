<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidsamsung">

    <!-- Feature declarations for VR and OpenGL ES -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="false"
        android:version="1" />
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <!--
      This <queries> block gives your app permission to find the Monado runtime.
      We need to list both the main package and the specific out-of-process service.
    -->
    <queries>
        <package android:name="org.freedesktop.monado.openxr_runtime" />
        <!-- THIS IS THE FIX: Added the specific service package from the error log -->
        <package android:name="org.freedesktop.monado.openxr_runtime.out_of_process" />
        <provider android:authorities="org.khronos.openxr.runtime_broker" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|keyboardHidden|screenSize|uiMode|density"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

            <meta-data android:name="android.app.lib_name"
                android:value="samsungproject" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
